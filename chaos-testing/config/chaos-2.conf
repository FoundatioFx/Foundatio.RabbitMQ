# RabbitMQ configuration for chaos testing node 2 (Aspire-managed)
# Independent node: medium constraints (350MB memory watermark)
# Used as failover target when chaos-1 is killed.

vm_memory_high_watermark.absolute = 350MB
disk_free_limit.absolute = 10MB
