{
  "name": "chaos_start_node",
  "description": "Start a previously stopped RabbitMQ chaos node container. The node will rejoin the cluster after starting. Allow 10-15 seconds for the node to fully initialize and rejoin.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "node": {
        "type": "string",
        "description": "The chaos node name (chaos-1, chaos-2, or chaos-3)",
        "enum": ["chaos-1", "chaos-2", "chaos-3"]
      }
    },
    "required": ["node"]
  },
  "annotations": {
    "title": "Start Chaos Node",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
