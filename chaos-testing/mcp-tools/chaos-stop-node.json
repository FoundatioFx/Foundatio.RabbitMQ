{
  "name": "chaos_stop_node",
  "description": "Stop (kill) a RabbitMQ chaos node container. This simulates a hard node failure - the container is killed immediately without graceful shutdown. Other cluster nodes will detect the failure and reorganize.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "node": {
        "type": "string",
        "description": "The chaos node name (chaos-1, chaos-2, or chaos-3)",
        "enum": ["chaos-1", "chaos-2", "chaos-3"]
      }
    },
    "required": ["node"]
  },
  "annotations": {
    "title": "Stop Chaos Node",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false
  }
}
