{
  "name": "chaos_clear_disk",
  "description": "Clear the disk alarm on a RabbitMQ chaos node by resetting disk_free_limit to 10MB. The alarm clears within a few seconds after this command.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "node": {
        "type": "string",
        "description": "The chaos node name (chaos-1, chaos-2, or chaos-3)",
        "enum": ["chaos-1", "chaos-2", "chaos-3"]
      }
    },
    "required": ["node"]
  },
  "annotations": {
    "title": "Clear Disk Alarm on Chaos Node",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
