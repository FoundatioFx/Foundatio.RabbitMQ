{
  "name": "chaos_cluster_status",
  "description": "Get the cluster status of a RabbitMQ chaos node. Returns JSON with running nodes, disk nodes, and partition information. Useful for verifying cluster health after chaos operations.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "node": {
        "type": "string",
        "description": "The chaos node name to query (chaos-1, chaos-2, or chaos-3)",
        "enum": ["chaos-1", "chaos-2", "chaos-3"]
      }
    },
    "required": ["node"]
  },
  "annotations": {
    "title": "Get Cluster Status",
    "destructiveHint": false,
    "idempotentHint": true,
    "readOnlyHint": true,
    "openWorldHint": false
  }
}
